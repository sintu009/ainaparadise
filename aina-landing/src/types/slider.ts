/** Single hero slider slide: id, title, background image path, CTA button label (e.g. "See our rooms"). */
export interface SliderSlide {
  id: number;
  title: string;
  bg: string;
  btnNext: string;
}
